// Import functions from React
import { Link } from "react-router-dom"

const Header = () => {
    return (
        <header>
            <Link></Link>
        </header>
    )
}

export default Header;